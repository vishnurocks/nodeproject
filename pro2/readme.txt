copy the code or pul the code with same file names and package.json file by copying it with same name 
and change the config in server.js file.
then Go to Terminal run command "npm i" used to install all the node modules
then run "npm run start"


